import { makeStyles } from '@material-ui/core';

const styles = {
  container: {
    display: 'flex',
    alignItems: 'center',
  },
  link: {},
  icon: {},
};

export const useStyles = makeStyles(styles, { name: 'GetStarted' });
