import { useStyles } from './LoadMultisafe.styles';

export const LoadMultisafe = () => {
  const classes = useStyles();
  return (
    <div className={classes.container}>
      <h1>Load existing Multisafe</h1>
    </div>
  );
};
