import { useStyles } from './CreateMultisafe.styles';

export const CreateMultisafe = () => {
  const classes = useStyles();
  return (
    <div className={classes.container}>
      <h1>Create Multisafe</h1>
    </div>
  );
};
